/** Unified agency panel path prefix (all agency types). */
export const AGENCY_PANEL_BASE = '/agency'

/** Dev-only: override effective role for RBAC UI (localStorage). */
export const DEV_AGENCY_ROLE_STORAGE_KEY = 'ontrip-dev-agency-role'
